#include<stdio.h>
#include<stdlib.h>


typedef struct Arvore{
No* raiz;
};


typedef struct No{

int chave;
No*esq;
No*dir;

};
void inicializa(Arvore* a){

a->raiz = NULL;
}
inserir(No* noVisitado, int chave){

No* inserir(No* noVisitado, int chave){

if(noVisitado == NULL){

    No*aux = new No;
    aux->esq = NULL;
    aux->dir = NULL;
    aux->chave = chave;
    return aux;

}
}
}



int main(){
arvore* a;
a=new arvore;
inicializa(a);
a->raiz = inserir(a->raiz, 16);
a->raiz = inserir(a->raiz, 12);
}
int numero;

inserir();
printf("Digite um valor ");
inserir(&n);
n-.raiz=inserir(n->raiz,16);
}
